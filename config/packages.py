import numpy as np
import matplotlib.pyplot as plt
import scipy as sp 
from fractions import Fraction

def fr(x):
    return Fraction(x).limit_denominator()

def frac(x):
    return Fraction(x).limit_denominator()
